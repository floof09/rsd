# Copy this to .env (same directory) and fill in real values.
# CodeIgniter reads these to configure the email transport.

# === Outbound Email (Hostinger Email) ===
email.fromEmail = noreply@yourdomain.com
email.fromName = RSD Notifications
email.protocol = smtp
email.SMTPHost = smtp.hostinger.com
email.SMTPUser = noreply@yourdomain.com
email.SMTPPass = CHANGE_ME
email.SMTPPort = 587
email.SMTPCrypto = tls
email.mailType = html
email.validate = true
# For Gmail, you can set allowAlias=true only if the alias is verified in Gmail settings
email.allowAlias = false

# === Alternative: Gmail SMTP (App Password) ===
# email.SMTPHost = smtp.gmail.com
# email.SMTPUser = your_workspace_account@yourdomain.com
# email.SMTPPass = APP_PASSWORD
# email.SMTPPort = 587
# email.SMTPCrypto = tls

# === Alternative: SendGrid / Mailgun / SES ===
# Use the SMTP host/port/user/pass from your provider
# and add their SPF/DKIM to DNS.
