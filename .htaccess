<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /rsd/
    
    # If not accessing public folder already
    RewriteCond %{REQUEST_URI} !^/rsd/public/
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
